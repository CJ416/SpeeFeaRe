## Core Insights
* Current discrete speech representations can be divided into semantic tokens and acoustic tokens, but not tailored for speech language models.

* Semantic distillation into the first codebook of RVQ codebooks.

* Repository code implementation adapted from Encodec, add semantic distill loss.

